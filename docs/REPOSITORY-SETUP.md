# Repository Setup

Recommended GitHub repository name: `larimia-frontend`.

Current remote supplied by project owner:
`https://github.com/appolon1908-hue/LARIM-A-Fornt-end`

```bash
git init
git branch -M main
git add .
git commit -m "feat: bootstrap LARIMIA frontend"
git remote add origin https://github.com/appolon1908-hue/LARIM-A-Fornt-end.git
git push -u origin main
```
